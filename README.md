# ordl-phetamines

Open Research and Development Laboratories - ordl-phetamines

## Clean-Room Platform Module

- New enterprise clean-room implementation lives in [`ordl_platform/`](ordl_platform).
- Backend: FastAPI + SQLAlchemy + policy-token enforcement.
- Frontend: React + TypeScript control UI shell.
- Runtime: Podman Compose stack (`ordl_platform/infra/podman-compose.yml`).
