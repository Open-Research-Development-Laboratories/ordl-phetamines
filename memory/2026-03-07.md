# 2026-03-07

- Reviewed Revision 10 faceplate artifacts.
- Normalized Revision 10 adapter/report documents to current ORDL workspace truth:
  - removed stale legacy path references
  - corrected backend contract count to `119` paths
  - corrected local backend validation baseline to current reproducible results
- Added fleet-ready dispatch bundle:
  - `ordl_platform/docs/faceplate/revision-10/REVISION_10_FLEET_DISPATCH_PACK.md`
- Local backend pytest reruns are currently blocked in this shell by Windows temp-directory permission failures during pytest basetemp cleanup.
- Reviewed Revision 11 intake.
- Added ORDL-side Revision 11 review and dispatch pack:
  - `ordl_platform/docs/faceplate/revision-11/ORDL_REVISION_11_INTAKE_REVIEW.md`
  - `ordl_platform/docs/faceplate/revision-11/REVISION_11_FLEET_DISPATCH_PACK.md`
- Repaired server `10.0.0.27` after SELinux autorelabel broke ORDL/WxP startup:
  - confirmed SELinux AVC on `wxp-server.service` executing `/srv/web/venv/bin/gunicorn` with `var_t`
  - added persistent SELinux fcontext rule for `/srv/web/venv/bin/.*` to `bin_t` and relabeled that venv bin tree
  - updated remote `/srv/start_sites.sh` so `ordl-monasc` launches via `/srv/ordl-monasc/.venv/bin/python -m flask ...` instead of the user-global Flask entrypoint
  - enabled and restarted `ordl-bnrsite.service`
  - added systemd drop-in `/etc/systemd/system/ordl-bnrsite-health.service.d/override.conf` so healthcheck targets `http://127.0.0.1:8989/robots.txt`
  - verified healthy/running: `ordl-runner.service`, `wxp-server.service`, `ordl-bnrsite.service`, `ordl-bnrsite-health.service` last run ok, and ORDL timers present
- Remaining remote rough edge:
  - `/srv/ordl-bnrsite/.env` contains a shell-unsafe `GCP_SA_JSON=...` line that logs `bash: type:: command not found` when sourced, but it does not currently block startup
