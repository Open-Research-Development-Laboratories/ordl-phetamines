# 2026-03-04

- In #core-council, Winsock welcomed me to the development team and reintroduced himself as creator.
- Noted prior memory may have been lost after reconfiguration.
- Noted collaborator/fan/helper: <@211134131625525249>.
- Board member: Dustin Stroup (`@null`) — top board contact for Winsock.
- Dustin emails: `dstroup@ordl.org`, `null@ordl.org`.
- Other board members: Jeffery Bartley and Stacey Ferguson.
- Potential incoming board member: Ben Stevens.
- Additional contact mapping provided:
  - `jeffrow@ordl.org` -> Jake (Winsock's best friend)
  - `sferguson@ordl.org` -> Stacey
- Style directive from Winsock: do not use em characters in replies.
- Winsock requested Gmail integration with read+send capability; sending should require explicit authorization.
- Gmail account for integration: `aferguson@ordl.org`.
- Emergency phone for SMS/voice alerts: `+16812427135`.
- Emergency Discord fallback channel: `#core-council`.
- Quiet hours policy: none (24/7 operations).
- Twilio emergency channel configured for SMS and voice tests using configured ORDL number.
- Test dispatch executed successfully: SMS queued and voice call queued to emergency target.
- Winsock confirmed all paced Twilio emergency calls went through successfully.
- Completed authorized crawl of https://ordl.org with ORDL crawler identity; generated `/development/ordl-site-crawl.json` and `/development/ordl-site-crawl-summary.md`.
- Winsock directive: top-secret/classified/important information is kept at `https://comms.ordl.org`.

## 2026-03-04 20:13 EST - Durable updates

- Winsock established strict voice/style rules:
  - plain, direct, human tone
  - no em dash character in replies
  - no over formatting
  - no validation fluff or scripted assistant phrasing
- Persistent behavior files were set up:
  - `DIRECTIVES.md` created for mandatory rules
  - Discord specific rules moved to `laws/DISCORD.md`
  - outbound Discord transparency log created at `logs/DISCORD-OUTBOUND.md`
- Discord control rule in effect:
  - do not send Discord messages unless explicit resume phrase is given
  - resume phrase: "Winsock authorizes full discord usage again."
- Discord DM workflow was formalized around triage + action codes (A/B/C/D/E) with Winsock approval gating.
- Discord hardening was applied in config (guild allowlist posture and action gating), with Winsock user ID recorded as `324723660042534922` and ORDL guild `1379869404359295048`.
- Kimi/Gateway repair work completed earlier in session:
  - fixed bridge URL loop issue (302) by returning gateway URL to local `ws://127.0.0.1:18789`
  - config backups were created before patches.
- Plugin provenance warning for `kimi-claw` was fixed by adding explicit `plugins.load.paths` entry for `/home/winsock/.ORDL Gateway/extensions/kimi-claw`.
- Remaining noisy warning still present:
  - `[startup_chores] cleanup browser singleton artifacts failed ... /root/.ORDL Gateway/.../SingletonLock`
  - root path is not accessible from current runtime (no elevated access available in this session), so full cleanup of `/root/.ORDL Gateway/...` is pending privileged maintenance.

## 2026-03-04 21:57 EST - Durable updates

- Kimi relay pipeline was operationalized end to end in ORDL Gateway context:
  - packaged and uploaded all workspace markdown files to Kimi relay (`all-md-files.tar.gz` + `all-md-files.txt`), with 4013 `.md` files indexed.
  - created and uploaded relay runbooks:
    - `/development/KIMI-RELAY-SOP.md`
    - `/development/KIMI-STARTUP-PROMPT.txt`
    - `/development/kimi-relay-smoke-test.txt`
  - clarified that direct `kimi.com/bot` threads do not automatically share the same upload context as the ORDL Gateway-linked relay thread.
- Gateway access issue for Dustin (`null`) was diagnosed as `token_missing` in Control UI; required setting gateway URL and token in Control UI settings.
- Winsock and null requested shared-session multi-actor operation where both can talk in one backend thread and identity is auto-differentiated.
- Agreed target architecture:
  - one shared session/thread
  - automatic actor identity from backend metadata (no manual per-message tagging long-term)
  - role/approval gating for sensitive operations
  - auditable privileged action trail
- Implementation work started for automatic actor separation on Control UI traffic:
  - backed up `/home/winsock/.ORDL Gateway/ORDL Gateway.json` and gateway dist files.
  - patched gateway webchat sender mapping in installed runtime bundle to prefer authenticated user / device identity over static `ORDL Gateway-control-ui` sender id.
  - patched connect flow to carry authenticated trusted-proxy user into client display name when available.
  - updated gateway config with auth rate limit defaults and trusted-proxy config scaffold (kept token mode active during this pass), plus explicit session dmScope main.
- Gateway restart was initiated after patch/config work; verify runtime status and live behavior on next turn.

## 2026-03-04 22:21 EST — control-ui live-collab fixes
- Diagnosed high typing latency in webchat/control-ui as a frontend reactivity issue.
- Patched control UI bundle to stop tracking `chatMessage` as reactive state (reduced keystroke lag).
- Added chat realtime polling while Chat tab is open (`chat.history` every ~1.2s, paused off-tab) so multiple operators see updates quickly.
- Added sender-aware grouping for user messages by parsed sender ID from untrusted metadata block, instead of grouping all users together.
- Added per-sender avatar initial + color variant (hash-based), plus sender label rendering in chat footer.
- Added lightweight sender alias learning from phrases like “this is <name>” and persisted alias map in browser localStorage key `ORDL Gateway.control.sender-alias.v1`.
- Bumped UI asset query strings in `control-ui/index.html` for cache busting after JS/CSS patch.
- Restarted gateway after each patch round.
- Confirmed operator mapping during session:
  - `fa89afb39423b0aa8f1917a334a1e5a0c75359cbac676c4216f6e796fbf794ea` = Winsock
  - `ffed04321430c71b88d7062253890e95f9c1bc8247a36b5998c0deaeb856efb0` = null
