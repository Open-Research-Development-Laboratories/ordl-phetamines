# 2026-03-05

- Confirmed Kimi host mapping:
  - Desktop (`ORDL Gateway-OPb`) -> `19cbcddf-d222-8483-8000-0000ab88f699`
  - Laptop (`ORDL Gateway-a7c`) -> `19cbcf0a-ca02-8803-8000-000008ecda2a`
- Locked role mapping:
  - `ordl-flint` = coordinator
  - `kimi-arch` = desktop worker
  - `kimi-labs` = laptop worker
- Added fleet policy docs in `/development/KIMI-FLEET-SETUP.md`.
- Updated `AGENTS.md` identity map with final worker IDs.
- Routing policy agreed with Winsock: desktop-first, laptop opportunistic, auto-failover to desktop-only if laptop drops.
- Hardened Discord behavior rules to reduce over-replying in group channels:
  - default silent mode in group channels unless directly asked/mentioned with clear request
  - explicit quiet keywords enforce NO_REPLY until explicit resume
  - casual chatter/status updates are not reply triggers
- Added `/development/laws/BOOK-MODE.md` and linked it from `DIRECTIVES.md` for strict by-the-book execution.
- Implemented and committed policy enforcement artifacts:
  - `specs/policy-gateway-architecture.md`
  - `specs/policy-schema.json`
  - `specs/policy-decision-table.md`
  - `tests/policy-scenarios.yaml`
  - `policy/engine.js`, `policy/gateway-server.js`, `policy/guarded-send.js`, `policy/alerts.js`, `policy/status-ui.html`
- Verified deterministic policy tests pass (5/5).
- Added block alert fanout and status signaling in policy gateway (Discord alert, optional email, red/yellow/green status output, reevaluate endpoint).
- Config safety lesson recorded: adding unsupported top-level key `policyGateway` to `~/.ORDL Gateway/ORDL Gateway.json` caused gateway startup failure; removed and recovered.
- Updated local gateway config on ghost with stricter Discord settings (dmPolicy pairing, allowFrom Winsock, requireMention true) and gateway auth rate-limit values.
- Completed multi-host node connectivity debugging:
  - Found desktop Windows+WSL runtime collision and standardized on WSL runtime path.
  - Identified node connection failures in sequence: EHOSTUNREACH, insecure ws guard, token mismatch, then pairing-required.
  - Found build-specific approval quirk: `ORDL Gateway devices approve` succeeded when `ORDL Gateway nodes approve <requestId>` returned unknown requestId.
  - Confirmed arch currently sees 1 connected node (desktop) in `ORDL Gateway nodes status`.
- Created automation scripts for repeatable fleet setup/verification:
  - `/development/scripts/fleet-bootstrap-arch.sh`
  - `/development/scripts/fleet-bootstrap-node.sh`
  - `/development/scripts/fleet-verify.sh`
- Sent requested Discord DM updates to null (`user:388680149622718464`) summarizing progress and coordination status.
- Confirmed current mesh state on arch remains `Known: 1 · Paired: 1 · Connected: 1` (desktop only).
- Clarified operational quirk: running node bootstrap on arch itself does not onboard other hosts; must execute bootstrap on each remote host (laptop/truth) locally.
- Hit tunnel conflict during mistaken local run (`bind [127.0.0.1]:55557: Address already in use`), reinforcing unique-port-per-host requirement.
- Found path mismatch on truth host: `/development/scripts/fleet-bootstrap-node.sh` missing there (scripts live on arch workspace), so remote hosts need copied script or inline setup commands.
- Prepared inline truth-host systemd node unit recipe using local forward + `ORDL Gateway_GATEWAY_TOKEN` + insecure-private-ws flag for current workaround path.

## ORDL clean-room platform build pass
- Implemented new clean-room module under ordl_platform/.
- Backend: FastAPI governance + policy + dispatch + digestion APIs with RBAC+ABAC.
- Added signed policy tokens, signed extension registration, and tamper-evident audit chain.
- Added worker inventory/action endpoints and code digestion coverage engine.
- Added React TypeScript UI shell and Podman Compose stack (Postgres/Valkey/MinIO/backend/worker/frontend).
- Added tests; backend suite passing (6/6).
- Added AGENTS/SESSION-SYSTEM-INSTRUCTIONS.md for repeatable session bootstrap (memory+MCP probe+fleet-first).

## 2026-03-05 23:xx EST - Codex standardization pass
- Ingested current OpenAI Codex docs pages for config, rules, AGENTS discovery, MCP, skills, multi-agent, speed, and docs index/sitemap.
- Rewrote workspace instruction standard to Codex-aligned syntax in:
  - /Users/Winsock/Documents/GitHub/ordl-phetamines/AGENTS/AGENTS.md
  - C:/development/AGENTS.md
- Added explicit precedence model, AGENTS discovery order, config/rules/MCP/skills/multi-agent standard sections, and preserved ORDL fleet role contract.
- Fixed fleet orchestrator restart/resync bug by changing self-matching kill pattern to pkill -f '[o]penclaw-gateway'.
- Resynced worker-build-laptop and worker-batch-server successfully with handshake/local-gateway-connected signals after patch.
- Added durable Codex docs memory pack: memory/codex-docs-reference.md and linked long-term MEMORY.md summary for quick future lookup.

## 2026-03-05 23:12 EST - Codex docs memory expansion
- Refreshed official Codex docs pages and expanded `memory/codex-docs-reference.md` into a full knowledge pack.
- Included standards and quick-reference sections for: config basics/advanced/reference/sample, speed, rules, AGENTS discovery, MCP, skills, and multi-agent.
- Ingested docs index sources (`https://developers.openai.com/`, `sitemap-index.xml`, `sitemap-0.xml`) and recorded sitemap count snapshot: 2,839 URLs.
- Mirrored the same file to `C:/development/memory/codex-docs-reference.md` for shared reuse.

## 2026-03-05 23:xx EST - Fleet orchestration health pass
- Upgraded Fleet API orchestration status path to collect worker status in parallel (`ThreadPoolExecutor`) for lower latency and better fault tolerance.
- Added fleet health evaluator endpoint: `GET /v1/fleet/health`.
- Health evaluation now checks:
  - desktop pending pairing count is zero
  - expected worker hosts are present in paired markers
  - per-worker process up + corpus presence
  - per-worker handshake/local-gateway signals are recent (configurable recency window)
  - critical signal errors are ignored if stale and only considered after latest success marker
- Improved signal normalization for JSON log lines from worker logs, reducing noisy raw payload output.
- Added config knobs:
  - `FLEET_STATUS_MAX_PARALLEL`
  - `FLEET_HEALTH_SIGNAL_RECENCY_MINUTES`
- Added helper tests under `fleet_api/tests/test_orchestrator_health_helpers.py` (3 passing).
- Updated playbooks recommended flow to include `/v1/fleet/health`.
