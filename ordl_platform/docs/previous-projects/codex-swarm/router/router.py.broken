                            s = c["text"].strip()
                            if s:
                                parts.append(s)
        return "
".join(parts).strip()
    return str(inp).strip()

